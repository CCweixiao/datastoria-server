# `next dev` runs on :3000 and calls the Java backend directly (the backend's
# dev CORS policy allows these origins). The statically exported production
# build is same-origin and does not use this file.
NEXT_PUBLIC_DATASTORIA_JAVA_API_BASE_URL=http://127.0.0.1:8081
