extends:
  - recommended

rules:
  # Authentication is currently documented by the route-specific
  # x-datastoria-auth extension rather than OpenAPI security schemes.
  security-defined: off
  # Some compatibility routes intentionally retain their legacy response set.
  operation-4xx-response: off
