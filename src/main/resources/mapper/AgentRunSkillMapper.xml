<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.datastoria.server.persistence.mapper.AgentRunSkillMapper">

  <resultMap id="PinMap" type="io.datastoria.server.persistence.entity.AgentRunSkillEntity" autoMapping="true"/>

  <insert id="insertAll">
    INSERT INTO ds_agent_run_skill (tenant_id, run_id, skill_id, skill_revision, content_checksum)
    VALUES
    <foreach collection="pins" item="p" separator=",">
      (#{p.tenantId}, #{p.runId}, #{p.skillId}, #{p.skillRevision}, #{p.contentChecksum})
    </foreach>
  </insert>

  <select id="findByRun" resultMap="PinMap">
    SELECT tenant_id, run_id, skill_id, skill_revision, content_checksum
    FROM ds_agent_run_skill
    WHERE tenant_id = #{tenantId} AND run_id = #{runId}
    ORDER BY skill_id
  </select>
</mapper>
